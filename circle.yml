general:
  artifacts:
    - out

dependencies:
  override:
    - go get github.com/jessevdk/go-flags

test:
  override:
    - ./build-all.sh

    - ./build.sh
    - tests/run.sh
    - rm -f out/cert-chain-resolver
