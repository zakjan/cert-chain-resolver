general:
  artifacts:
    - out

machine:
  environment:
    GO15VENDOREXPERIMENT: 1

dependencies:
  override:
    - ./circle.sh dependencies

test:
  override:
    - ./circle.sh build
    - ./circle.sh test
    - ./circle.sh release
